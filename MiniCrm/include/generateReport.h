#ifndef GENERATEREPORT_H
#define GENERATEREPORT_H

/**
 * @brief Генерация аналитического отчета.
 *
 * Формирует текстовый отчет на основе данных из clients.bin:
 * 1. Группирует клиентов по товарам
 * 2. Сортирует:
 *    - Товары по алфавиту
 *    - Покупки внутри групп по дате
 * 3. Определяет:
 *    - Самый популярный товар (по количеству)
 *    - Самого активного клиента (по сумме покупок)
 *
 * Результат сохраняется в report.txt и дублируется в консоли.
 */
void generateReport();

#endif